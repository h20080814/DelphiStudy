program Project1;

{$APPTYPE CONSOLE}

{$R *.res}
{*------------------------------------------------------------------------------
方法：
    1、种类：
      1、静态方法：  类中缺省类型，执行速度快，不支持多态性
      2、虚拟方法：
      3、动态方法：
      4、类方法：
      5、抽象方法：
      6、消息方法：
      7、构造方法：
      8、析构方法：
-------------------------------------------------------------------------------}
uses
  System.SysUtils,
  Unit1 in 'Unit1.pas';

var
  Person: TPerson;

begin
  Person := TPerson.Create();
  Person.Talk();
  Person.Free;
  Readln;
end.

