program Project1;

{$APPTYPE CONSOLE}

{$R *.res}
{*------------------------------------------------------------------------------
 指针： 存储了内存地址的变量
      作用：效率高
          @ 获取地址
          ^ 定义指针
      分类：
          类型指针  具有明确的数据类型
            使用方法：
                1、 1、定义/声明一个整数类型的指针
                   var
                      PInt:^Integer ; //定义一个整数类型的指针
                              ^Integer:=PInteger  等同
                            PInteger是封装好的指针类型
                2、定义/声明一个变量
                  var
                      i:integer;  //整数型变量
                3、把变量i的地址赋给指针PInt
                  begin
                    PInt:=@i;
                    Writeln(Integer(PInt));
          无类型指针  没有数据类型 ，可以指向任何地址
      使用方法：

          1、定义/声明一个指针
              var
                 viod: Pointer;
-------------------------------------------------------------------------------}
uses
  System.SysUtils;

var
  i: Integer; //整数型变量

var
  PInt: PInteger;

var
  viod: Pointer;

begin
  //i:=100;
  PInt := @i;
  Writeln(Integer(@i));
  Writeln(Integer(PInt));
  Readln;
end.

