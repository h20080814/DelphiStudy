program ProjectDemo1;

{$APPTYPE CONSOLE}

{$R *.res}

uses
  System.SysUtils;

begin
 {
    注释
 }
 // Delphi学习c系语言的注释方式
 {*
   注释
   命名规范
    1、见名知意：尽量使用英文。
    2、驼峰原则：
      大驼峰  StudentName    每个单词的首字母大写
      小驼峰  studentName     由第二个单词开始，每个单词的首字母大写

    数据类型：
    1、为什么要定义类型
         为了更加合理、科学的使用计算机存储空间
    2、有那些类型
      1）数值型
          整数
          小数（实数）
      2）逻辑型
          真（true)  非0为真
          假 （flase)  0
      3)指针类型
      4）数组、子界、集合、自定义类型（结构体、类）
      5）字符串
    3、类型转换
      1）四舍五入
      2）自动类型转换：取值范围小的数据类型向取值范围大的数据类型进行转换。例如：整数转小数
      3）强制类型转换：取值范围大的数据类型向取值范围小的数据类型进行转换。例如：小数转整数
          格式：要转换成的数据类型（等待被转换的类型）
    运算符
    1、变量    编程中最小的存储单元（空间），它的空间大小由它在声明时的数据类型决定
      1)声明  ：定义一个变量，告诉Delphi我们要使用一个名字是xxx,空间大小是xxx的内存空间
          var 变量名：数据类型;
      2)初始化  给变量的第一次赋值
          变量名:=变量的值;
    2、运算符
      算术运算符：=、-、*、/（不会整除，有小数点）、div(整除，只取商，不四舍五入）、mod(求模,求余数）

 *}
  Writeln(6/4);
  Writeln(6 div 4);
  Writeln(61 mod 4);

  Readln;

end.

